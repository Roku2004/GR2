server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: "D:/GR2/config-repo"
          clone-on-start: true

management:
  endpoints:
    web:
      exposure:
        include: "*"